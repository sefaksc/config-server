server:
  port: 8085
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cartdb
    username: root
    password: sefa1220504
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
