server:
  port: 8083
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/productsdb
    username: root
    password: sefa1220504
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
